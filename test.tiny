
proc main(length)
	local arr = [$length]
	
	local i = 0
	while $i < len($arr)
		$arr[$i] = $i
		$i = $i + 1
	end
	
	$i = 0
	while $i < len($arr)
		write $arr[$i] end
		$i = $i + 1
	end
	
	exit(1)
end

length = 0

read length end
length = tonumber(length)
main(length)
